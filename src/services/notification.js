// Client-side notification helper routines.
// Communicates with Resend proxy to send notification updates.

export async function sendResolutionNotification(data) {
  console.log('Sending resolution notification...', data);
  return null;
}
